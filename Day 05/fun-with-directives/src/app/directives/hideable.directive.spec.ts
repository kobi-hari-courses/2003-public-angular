import { HideableDirective } from './hideable.directive';

describe('HideableDirective', () => {
  it('should create an instance', () => {
    const directive = new HideableDirective();
    expect(directive).toBeTruthy();
  });
});
